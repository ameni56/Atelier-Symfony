<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des livres</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <!-- Header Section -->
    <header class="bg-warning text-white text-center py-3 mb-4">
        <h1 class="display-4">Bienvenue à la Liste des Livres</h1>
    </header>

    <!-- Main Content -->
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h2 class="text-center mb-4">Liste des livres publiés entre 2014 et 2018</h2>

                <div class="mb-3 text-center">
                    <a href="{{ path('app_add_book') }}" class="btn btn-primary">Ajouter un Livre</a>
                </div>

                {% if books is not defined or books is empty %}
                    <div class="alert alert-warning" role="alert">
                        <h4 class="alert-heading">Aucun livre trouvé dans cette période.</h4>
                    </div>
                {% else %}
                    <!-- Responsive Table -->
                    <div class="table-responsive">
                        <table class="table table-dark table-striped table-bordered text-center">
                            <thead class="thead-light">
                                <tr>
                                    <th>Titre</th>
                                    <th>Date de publication</th>
                                    <th>Publié</th>
                                    <th>Catégorie</th>
                                    <th>Auteur</th>
                                    <th>Modifier</th>
                                    <th>Supprimer</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for book in books %}
                                    <tr>
                                        <td>{{ book.title }}</td>
                                        <td>{{ book.publicationDate|date('Y-m-d') }}</td>
                                        <td>{{ book.published ? 'Oui' : 'Non' }}</td>
                                        <td>{{ book.category }}</td>
                                        <td>{{ book.author.username }}</td>
                                        <td><a href="{{ path('app_update_book', { id: book.id }) }}" class="btn btn-sm btn-outline-light">Modifier</a></td>
                                        <td><a href="{{ path('app_delete_book', { id: book.id }) }}" class="btn btn-sm btn-outline-danger">Supprimer</a></td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>

                  
                {% endif %}
            </div>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
