<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recherche d'Auteurs</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <!-- Header Section -->
    <header class="bg-primary text-white text-center py-3 mb-4">
        <h1 class="display-4">Recherche d'Auteurs</h1>
    </header>

    <!-- Search Form -->
    <div class="container">
        <form method="get" action="{{ path('app_search_authors') }}" class="form-inline justify-content-center mb-4">
            <div class="form-group mr-2">
                <label for="minBooks" class="mr-2">Min Livres</label>
                <input type="number" id="minBooks" name="min_books" value="{{ minBooks }}" class="form-control" min="0" placeholder="Min Livres">
            </div>
            <div class="form-group mr-2">
                <label for="maxBooks" class="mr-2">Max Livres</label>
                <input type="number" id="maxBooks" name="max_books" value="{{ maxBooks }}" class="form-control" min="0" placeholder="Max Livres">
            </div>
            <button type="submit" class="btn btn-primary">Rechercher</button>
        </form>

        {% if authors is not defined or authors is empty %}
            <div class="alert alert-warning text-center">
                Aucun auteur trouvé pour cette plage de livres.
            </div>
        {% else %}
            <table class="table table-striped table-bordered">
                <thead class="thead-dark">
                    <tr>
                        <th>Auteur</th>
                        <th>Nombre de Livres</th>
                    </tr>
                </thead>
                <tbody>
                    {% for author in authors %}
                        <tr>
                            <td>{{ author[0].name }}</td>
                            <td>{{ author.book_count }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% endif %}
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
